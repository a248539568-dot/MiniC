# 安全政策

English version: [Security Policy](docs/en/SECURITY.md)

## 支持范围

安全修复优先面向最新发布版本。项目尚未建立长期支持分支；报告中请注明受影响版本和 Windows 版本。

## 私下报告

请不要为可被利用的安全问题直接创建公开 Issue。仓库发布到 GitHub 时，维护者应先启用 GitHub Private Vulnerability Reporting，并将其作为默认报告渠道。在该渠道启用前，请联系仓库维护者并等待其提供私密沟通方式。

报告应包含受影响版本、复现条件、潜在影响和尽可能小的验证样本。不要发送真实私人桌面文件。

## 敏感数据

MiniC 不保存文件内容，但 `%LOCALAPPDATA%\MiniC\layout.json` 包含本机完整路径，可能暴露用户名、目录和文件名。提交 Issue、日志或测试夹具前必须脱敏。

应重点报告以下问题：

- 非预期覆盖、移动、删除或泄露桌面文件
- 路径校验缺陷、目录穿越或对公共桌面的越权写入
- Shell 命令、快捷方式或文件名导致的命令执行
- 安装、升级或开机启动流程中的权限提升
- 异常退出后无法恢复 Explorer 桌面，导致用户无法正常操作

一般功能缺陷、视觉问题和不包含安全影响的崩溃可使用普通 Issue。

## 处理原则

维护者确认问题后应限制披露范围、准备修复和回归验证，并在可用版本发布后再协调公开说明。安全修复不得通过静默删除用户文件或布局来规避问题。

# 版本记录

本文件记录面向用户和贡献者的重要变化。更细的逐次开发记录保留在 `development.log`。

## [Unreleased]

- 待维护者选择并添加项目开源许可证。

## [0.59.0] - 2026-09-17

- 修复 Photoshop 等外部程序保存到桌面后未自动出现的问题：文件系统监听与 Shell 通知并行，并增加后台元数据差异检查；刷新期间的新请求不再丢弃。
- 回收站独立查询真实空/满状态，使用专用图标更新路径，避免通用 Shell 图标缓存和异步覆盖导致清空后仍显示有内容。
- 收纳盒标题栏、名称、按钮及标签点击统一激活到前台，保留原拖动、重命名和非永久置顶行为。
- 桌面和收纳盒内文件名失去焦点即提交，支持点击其他位置、切换窗口或标签；Enter 仍可提交、Escape 取消，失败保留原文件，重复失焦事件不会重复改名或抢回焦点。

## [0.58.9] - 2026-09-13

- 收纳盒拖动预览保留原配色、材质和透明度，去掉提亮底板及内容淡化。
- 合并提示仅使用主题色细边框高亮，背景保持透明，原盒与目标内容保持原样式。

## [0.58.8] - 2026-09-13

- 修复收纳盒拖动反复重启位置/透明度动画和调整透明窗口尺寸导致的拖尾与卡顿；改为逐帧跟手和稳定预览视口。
- 移除预览落地后的重复内容滑入，合并/拆分仅差量同步宿主；重复排版不再重启标签和图标换位动画。

## [0.58.7] - 2026-09-13

- 标签左右拖动固定为排序，上下拖动用于抽离和合并，手势开始后锁定方向，避免抖动误操作。
- 合并仅由目标标签栏接收，独立盒使用顶部名称区域；提示高亮与实际命中范围一致。
- 释放时重新核对合并目标，移回源标签栏不改变标签顺序。

## [0.58.6] - 2026-09-13

- 收纳盒合并与拆分改为等尺寸卡片抽离、柔性连接断开和定向滑入融合，移除原有整盒缩放。
- 支持拖回及 Escape 取消；中断或关闭预览后不会继续提交合并，预览与实际拆出落点统一。
- 单标签预览只携带自身内容，目标内容淡出提示接收，并遵循系统动画开关。

## [0.58.5] - 2026-09-10

- 修复桌面与收纳盒双击按松开时间判断、非本图标按下也能误触拖动的问题；刷新文件元数据时保留图标对象和选择状态。
- 重命名不再临时隐藏整个桌面层；提交期间延后刷新并保留变化通知，结束编辑时避免反复切换窗口层级。
- 桌面自动排列、拖放和已有位置修复按每个显示器的工作区计算，避开任务栏及显示器间空洞；考虑系统缩放并限制底行长文件名的显示范围。
- 修复桌面和收纳盒左键启动后快捷键无效、只有打开右键菜单后才生效的问题；复制和剪切项目恢复普通选中样式，并使用同色同规格的对称矢量标识。
- 收纳盒点击后按普通活动窗口进入前台，切换到其他应用后退回桌面层；MiniC 全屏桌面图标层始终保持在普通应用下方。
- 桌面系统“新建”子菜单中的所有已注册文件类型与文件夹都落到右键菜单触发点所在网格，占用时使用附近最近空格。

## [0.58.4] - 2026-09-03

- 桌面变化改用 Windows Shell 通知驱动，周期协调不再每秒全量扫描；注册失败时保留文件系统监听回退。
- 修复 Shell 通知入口名错误导致的闪退，并增加不会反向抛出异常的本地诊断日志。
- 清理废弃方法、无调用的 Win32 声明和旧剪贴板判断，Shell 通知注册及资源生命周期迁入独立服务。
- 固化 .NET 推荐分析器、警告即错误和整数溢出检查，防止新的构建告警进入发布版本。
- 桌面层级正确时不再改写 Z 序，无锚点时以 Explorer 桌面宿主回退，避免刷新或重命名时覆盖普通应用。
- 补齐常用文件快捷键、快捷方式重命名规则、新建后名称确认及 Shell 原生剪贴板传输。

## [0.58.3] - 2026-09-02

- 重命名编辑器改为只替换图标下方的原文件名，不再横向撑开为长输入框或遮挡图标；桌面与收纳盒使用一致的原位编辑样式。
- 修复收纳盒图标随文件名行数上下偏移的问题，同一行图标现在保持统一顶部基线。

## [0.58.2] - 2026-09-02

- 调整桌面右键菜单层级：“刷新桌面”移到一级菜单，`MiniC` 子菜单显示应用图标并保留收纳盒操作。

## [0.58.1] - 2026-09-02

- 桌面空白右键菜单增加 `MiniC` 子菜单，可直接新建收纳盒、刷新桌面及显示或隐藏收纳盒，同时保留完整 Windows Shell 菜单。

## [0.58.0] - 2026-09-01

- 桌面空白右键改为直接调用 Windows Shell 的 Desktop 文件夹背景菜单，删除隐藏 Explorer 图标层的坐标穿透。
- 修复连续新建失败、无法再次新建文件夹，以及新项目误建到隐藏文件夹内的问题。

## [0.57.9] - 2026-09-01

- 系统菜单交接期间暂停 MiniC 桌面刷新，并尝试提交 Explorer 遗留的原生重命名会话。

## [0.57.8] - 2026-09-01

- 修复从 MiniC 桌面连续使用系统“新建”时同名文件创建失败的问题，保留 Windows 自动追加序号的命名行为。

## [0.57.7] - 2026-09-01

- MiniC 桌面层现在跟随 Windows“自动排列图标”设置压紧排列未收纳项目，收纳盒不参与排列。
- 延长 Explorer 桌面空白右键菜单的交互接管时间，让系统“新建”完成同名避让和文件创建。

## [0.57.6] - 2026-09-01

- 新建、下载或另存为到桌面的文件改为按时间和名称稳定填入 MiniC 的下一个空网格，不再跟随隐藏 Explorer 的随机落位。

## [0.57.5] - 2026-09-01

- 修复软件更新短暂删除并重建桌面快捷方式后，原收纳盒图标跑到 MiniC 桌面的问题。

## [0.57.4] - 2026-09-01

- 修复清空回收站后仍显示“有文件”图标的问题，改为根据实际项目数选择 Windows 空/满回收站系统图标。

## [0.57.3] - 2026-09-01

- 统一标签拖入和拖出的整盒动画：拖出放大、拖入缩小，并避免窗口边界裁切。
- 合并与拆出期间不再把动画尺寸写回布局模型。

## [0.57.2] - 2026-09-01

- 修复标签合并后宿主窗口漏建导致收纳盒和图标不可见的问题。
- 增加标签拓扑原子更新、完整性校验、失败回滚和窗口覆盖自愈。

## [0.57.1] - 2026-09-01

- 以 Explorer 和 DWM 就绪检测替代固定启动等待，缩短开机启动首帧时间。
- 收纳盒先显示，旧数据迁移和完整文件元数据刷新在后续完成。

## [0.57.0] - 2026-09-01

- 重做标签拖动状态：源盒内换序，拖出后放大，进入目标盒后缩小，释放时才提交。
- 合并只移动当前标签，并追加到目标标签末尾。

## [0.56.7] - 2026-09-01

- 新增 Explorer 桌面回退守护，主程序崩溃或被强制结束后恢复系统图标层。

## [0.56.6] - 2026-09-01

- 修复移动收纳盒经过无目标桌面区域时可能崩溃的问题。

## [0.56.5] - 2026-09-01

- 去除收纳盒常驻边框，标题栏非按钮区域均可拖动窗口。

## [0.56.2] - 2026-09-01

- 新桌面项目只占用剩余网格，不再改变已有图标位置和顺序。

## [0.56.1] - 2026-09-01

- 修复透明窗口黑角，改用圆角一致的模拟液态玻璃材质。

## [0.56.0] - 2026-09-01

- 增加标题原位重命名、单选全名、列表预览、文件夹和回收站投放。
- 增加资源管理器到桌面或收纳盒的拖入，并修复重复投放产生副本的问题。

早期版本的详细记录可查阅 `development.log`。

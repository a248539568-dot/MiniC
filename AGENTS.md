# 业务逻辑
- 业务逻辑梳理，优先使用已有且成熟的的业务逻辑设计经验
# 开发技术技术选型
- 按用户具体需求进行分析最适合的开发技术选型
## 桌面
- tauri + vue3
- electron + vue3
- flutter
- 其他桌面开发组合
## web开发
- golang + vue3
- fastapi + vue3
- 注意：Python 必须创建虚拟环境进行开发
## 小程序
- 微信使用微信小程序开发推荐的技术选型
## 人工智能（本地模型及算法）
- 开发人工智能推荐使用Python或者node 或者两者搭配使用，主要用于后端，如ai大模型部署调用
- 注意：Python 必须创建虚拟环境进行开发

# 开发规范
- 不能瞎猜技术实现，必须配合联网搜索相关技术文档调研（最新和最稳）
- 让代码更容易去承载复杂的业务逻辑，使代码简简洁易懂，具有高内聚、低耦合特性，能够容易扩展、更新。
## 开发原则solid
- 规划清晰的项目目录结构，为了提高扩展性，每个层都要有对应的文件目录
- 严格遵守单一职责、开放封闭原则、依赖倒置原则、里氏替换原则、接口隔离原则、迪米特原则、合成复用原则
- 设计模式（Design Patterns - Elements of Reusable Object-Oriented Software）
## 修改代码
- 在陌生代码修改中，必须梳理代码的因果关系，禁止瞎猜，盲目的修改代码。
- 修改代码时进行关联性检查，有关联进行就行关联性修改（本条在次强调以上规范）
- 禁止盲猜代码实现，必须做好联网搜索，优先使用官方文档，在看技术社区等文章。
- 在重构和编写新代码，不能遗留旧代码。
## 开发日志
- 简单记录新增、修改、删除逻辑，包含时间等关键信息记录，简单易读。
- 保存在项目根目录下，保存.log文件。
## 禁止
- 不能为了实现而不遵守开发规范
## 网络安全
- 使用oath认证、jwt双token标准
- 禁止跨站脚本攻击、跨站请求伪造、数据库注入、代码逻辑漏洞（修改权限、密码）、权限绕过、权限伪造、文件访问权限绕过伪造...等安全问题
- 网络payload测试、渗透测试。

# UI设计规范
## 统一
- 重点是让用户在视觉上感到舒适和减少认知负担
- 联网搜索最新的流行趋势（当前流行那种样式）
- 要求自适应、弹性化布局
- 减少边框的使用，大致范围界定例如：文本框、下拉选择框是需要边框的；容器、按钮等一般不需要边框。
- 按板块、功能等进行划分区域，比例均匀
## 排版
- 排版遵守：对比、对齐、重复、亲密性原则
- 用间距划分不同的容器
- 禁止在页面堆叠功能性交互组件，简化操作流程，减少用户认知负担
- 将功能合理分配，常用功能在主界面呈现，不常用功能必须收纳（例如设置弹窗面板、折叠面板等）
## 布局
- 必须合理、符合人类认知习惯的布局
- 在开发功能性应用，文字说明和展示比例约10%，大部分区域留给功能
- 容器、盒子等加轻微阴影
- 使用间距体现亲密关系（即便使用到分割线条，也要降低颜色饱和度）
## UI组件、表单、表格、输入、按钮等
- 自定义ui组件，禁用系统组件。
- 滚动条不能使用默认的滚动条样式，优先是细滚动条，淡色。
- 在需要使用滚动条的容器（如多内容的情况）鼠标划入时才显示滚动条，滚动条预留一定的空间，避免划入时页面抖动。
## 文字
- 文字与图标必须对齐大小相同，且有一定微笑间距
- 禁止使用粗体
## 配色
- 布局配色主要用于能够识别区域
- 状态色采用中性色
- 背景色、容器背景色、组件配色，色系统一，不要有割裂感
## 动效
- 动效主要用于用于的操作状态的提示，动画要流畅，不能过于生硬
- 每个组件应当都有动效，如状态改变、鼠标划过等
- 动画空间预留、不能使页面有跳动感、保持平滑过渡
## 图标
- 禁止使用字符来代替图标
## 交互
- 交互必须简单、苹果级的交互逻辑
- 防呆设计、确保用户在操作过程中不会出错（多种可能性错误）

## ui验证
- 截图查看验证，修改细节等
